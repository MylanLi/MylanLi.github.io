import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: '#304674',
    },
    secondary: {
      main: '#c6d3e3',
    },
    background: {
      default: '#d8e1e8',
    },
  },
  typography: {

  },
});

export default theme;